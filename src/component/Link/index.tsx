import React from "react";
import { Link as RouterLink } from "react-router-dom";
import { Link as MuiLink } from "@mui/material";
const Link = () => {
  return <RouterLink to={"/link"}>link</RouterLink>;
};

export default Link;
